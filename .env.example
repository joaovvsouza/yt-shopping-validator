# =============================================================================
# Variáveis de Ambiente - YouTube Shopping Validator
# =============================================================================
# Copie este arquivo para .env e preencha com os valores corretos

# =============================================================================
# OAuth e Autenticação
# =============================================================================
# URL do servidor OAuth
OAUTH_SERVER_URL=https://your-oauth-server.com

# ID da aplicação (App ID)
VITE_APP_ID=your-app-id

# Secret para assinar cookies JWT
JWT_SECRET=your-jwt-secret-key-change-in-production

# OpenID do proprietário/admin da aplicação
OWNER_OPEN_ID=your-owner-open-id

# URL do portal OAuth (para login)
VITE_OAUTH_PORTAL_URL=https://your-oauth-portal.com

# =============================================================================
# Banco de Dados
# =============================================================================
# URL de conexão MySQL (formato: mysql://user:password@host:port/database)
DATABASE_URL=mysql://user:password@localhost:3306/yt_shopping_validator

# =============================================================================
# Forge API (para storage e mapas)
# =============================================================================
# URL base da API Forge (backend)
BUILT_IN_FORGE_API_URL=https://forge.butterfly-effect.dev

# Chave da API Forge (backend)
BUILT_IN_FORGE_API_KEY=your-forge-api-key

# Chave da API Forge (frontend - para mapas)
VITE_FRONTEND_FORGE_API_KEY=your-frontend-forge-api-key

# URL da API Forge (frontend - opcional, padrão: https://forge.butterfly-effect.dev)
VITE_FRONTEND_FORGE_API_URL=https://forge.butterfly-effect.dev

# =============================================================================
# Analytics (Opcional)
# =============================================================================
# Endpoint do Umami Analytics
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com

# Website ID do Umami
VITE_ANALYTICS_WEBSITE_ID=your-website-id

# =============================================================================
# Servidor
# =============================================================================
# Porta do servidor (padrão: 3000)
PORT=3000

# Ambiente (development ou production)
NODE_ENV=development

# =============================================================================
# Puppeteer/Chromium (Opcional)
# =============================================================================
# Caminho personalizado para o executável do Chromium/Chrome
# Se não especificado, o sistema tentará detectar automaticamente
CHROMIUM_PATH=/usr/bin/chromium-browser

# Pular download do Chromium pelo Puppeteer (usar @sparticuz/chromium no Railway)
# Configure como "true" no Railway para usar o Chromium do nixpacks.toml
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
